#include <stdio.h>

int main()
{
      int a;
      printf("etner value a: ");
      scanf("%d", &a);
      while (a >= 1)
      {
            printf("value : %d\n", a);
            a -= 2;
      }

      /* code */
      return 0;
}
